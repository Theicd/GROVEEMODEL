# Grove Search Companion — OpenSERP config (bundled with GROVEEMODEL plugin)
server:
  host: 127.0.0.1
  port: 7000
  debug: false
  verbose: false
  raw_requests: true
  insecure: true

app:
  log_format: text
  timeout: 15
  browser_path: ""
  head: false
  max_processes: 4
  idle_ttl: 5m
  mega_timeout: 90s

extract:
  enabled: true
  default_mode: auto
  timeout: 20s
  max_bytes: 2000000
  max_concurrent: 2

cache:
  ttl_seconds: 120
  max_size: 500

resilience:
  max_retries: 1
  allow_endpoint_fallback: false

cors:
  enabled: true
  allow_origins: "*"
  allow_methods: "GET, POST, OPTIONS"
  allow_headers: "Origin, Content-Type, Accept, Authorization, X-Use-Proxy"
  max_age: 86400

bing:
  rate_requests: 60
  rate_burst: 3

duckduckgo:
  rate_requests: 60
  rate_burst: 3

ecosia:
  rate_requests: 60
  rate_burst: 3
