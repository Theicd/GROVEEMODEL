import { GEMMA_ESTIMATED_BYTES } from "../introProgressFormat";

/** Same model as chat + «סכם כתבה» (model.worker). */
export const GEMMA_ARTICLE_MODEL_ID = "onnx-community/gemma-4-E2B-it-ONNX";

export { GEMMA_ESTIMATED_BYTES };
